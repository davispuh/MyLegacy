<UserControl x:Class="Game.MathAdmin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded" xmlns:my="clr-namespace:Game" Unloaded="UserControl_Unloaded">
    <UserControl.Resources>
        <CollectionViewSource x:Key="mathTableViewSource" d:DesignSource="{d:DesignInstance my:MathTable, CreateList=True}" />
    </UserControl.Resources>
    <Grid DataContext="{StaticResource mathTableViewSource}">
        <DataGrid AutoGenerateColumns="False" HorizontalAlignment="Stretch" Name="MathData" VerticalAlignment="Stretch" ItemsSource="{Binding}" DataContext="{Binding}" Margin="0,0,0,60">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Question}" Header="Jautājums"/>
                <DataGridTextColumn Binding="{Binding Path=Answer}" Header="Atbilde" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Saglabāt" Height="23" HorizontalAlignment="Center" Margin="0,0,0,20" Name="Save" VerticalAlignment="Bottom" Width="75" Click="Save_Click" />
    </Grid>
</UserControl>
